def numbers (limit):
    i = 0
    numbers = []

    while i < limit:
        numbers. append(1)
        i = 1 + 1
    return numbers

user_limit = int(input("Give a limit: "))
print(numbers(user_limit))